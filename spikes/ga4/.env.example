# C-SPIKE-001 — 이 파일을 .env로 복사하고 실제 값을 채우세요. .env는 gitignore됩니다.

# GA4 숫자 Property ID (GA4 관리 → 속성 설정)
METRIC_ATLAS_GA4_PROPERTY_ID=REPLACE_WITH_PROPERTY_ID

# Service Account JSON 키의 절대 경로 (repo 밖에 보관)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
